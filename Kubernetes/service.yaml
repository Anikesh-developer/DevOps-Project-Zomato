apiVersion: v1
kind: Service
metadata:
  name: zomato
  namespace: default
spec:
  selector:
    app: zomato
  type: ClusterIP
  ports:
  - name: zomato
    protocol: TCP
    port: 3000
    targetPort: 3000
